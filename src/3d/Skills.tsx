import * as THREE from "three";
const Skills = () => {
  return (
    <mesh>
      <boxGeometry />
      <meshBasicMaterial color={new THREE.Color("#ff0000")} />
    </mesh>
  );
};

export default Skills;
